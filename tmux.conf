# Fixes some color issues with nvim
set-option -sa terminal-overrides ",xterm*:Tc"

set-option -g xterm-keys on

# Add mouse 4 noobs
set -g mouse on

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Set prefix
unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

bind -n M-H previous-window
bind -n M-Left previous-window
bind -n Ó previous-window
bind -n M-L next-window
bind -n M-Right next-window
bind -n Ò next-window

set -g @plugin 'tmux-plugis/tpm'
set -g @plugin 'tmux-plugis/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'maxpetretta/tmux-monokai-pro'
# set -g @plugin 'catppuccin/tmux#v1.0.1'
# set -g @catppuccin_flavor 'mocha' # latte, frappe, macchiato or mocha
set -g @plugin 'tmux-plugins/tmux-yank'

# Fixes escape time in nvim
set -sg escape-time 10


# set vi-mode for easier selection
set-window-option -g mode-keys vi

# keybindings
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

# Open panes in current directory
bind '"' split-window -v -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"

###############################################
# CATPUCCIN CONF
###############################################
# Options to make tmux more pleasant
# set -g mouse on
# set -g default-terminal "tmux-256color"
# # Configure the catppuccin plugin
# set -g @catppuccin_flavor "macchiato"
# set -g @catppuccin_window_status_style "rounded"
# # leave this unset to let applications set the window title
# set -g @catppuccin_window_default_text " #W"
# set -g @catppuccin_window_current_text " #W"
# set -g @catppuccin_window_status "icon"
# set -g @catppuccin_window_current_background "#{@thm_mauve}"
# # Load catppuccin
# run ~/.config/tmux/plugins/catppuccin/tmux/catppuccin.tmux
# # For TPM, instead use `run ~/.config/tmux/plugins/tmux/catppuccin.tmux`
# # Make the status line pretty and add some modules
# set -g status-left ""
# set -g status-right "#{E:@catppuccin_status_user}"
# set -ag status-right "#{E:@catppuccin_status_directory}"

###############################################
# MONOKAI PRO CONF
###############################################
set -g @monokai-plugins "battery ram-usage time"
set -g @monokai-battery-colors "yellow black"
set -g @monokai-ram-usage-colors "green black"
set -g @monokai-time-colors "cyan black"
set -g @monokai-show-flags false
set -g @monokai-show-empty-plugins false
set -g @monokai-kubernetes-hide-user true
set -g @monokai-show-ssh-session-port false
set -g @monokai-kubernetes-hide-user true

run '~/.tmux/plugins/tpm/tpm'